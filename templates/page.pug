
extends layout

block append vars
  - bodyclass = 'project'

block title
  | #{page.title}

block header
  h1= page.title

block content
  section.content!= page.html

block append footer
  link(rel='stylesheet', href=contents.styles['code.scss'].url)
